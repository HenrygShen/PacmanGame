import javafx.scene.canvas.Canvas;
import javafx.scene.canvas.GraphicsContext;;

public class GameThread extends Thread {
	
	GameScene gameScene;
	private boolean running;
	private GraphicsContext graphicsContext;
	
	public GameThread(GameScene gameScene,GraphicsContext graphicsContext,Image character) {
		
		this.gameScene = gameScene;
		this.running = false;
		this.graphicsContext = gameScene.getGC();
		
	}
	
	
	@Override
	public void run()  {

	        while (this.running) {

	        	graphicsContext.clearRect(0, 0, 1440, 900);

	       
	            	
	    			value++;
	    			
	    			graphicsContext.drawImage( pacman, value, value );
	    		    mainStage.setScene(scene);

	          
	            }
	            
	        }
	    }

	    public void setRunning(boolean running)  {

	        this.running= running;
	    }
	}
