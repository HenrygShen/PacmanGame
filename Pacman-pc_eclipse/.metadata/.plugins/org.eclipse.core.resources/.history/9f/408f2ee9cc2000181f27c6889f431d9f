import javafx.scene.canvas.GraphicsContext;
import javafx.scene.image.Image;

/**
 * Class currently has no animation.
 */

public class Ghost extends GameObject {
	
	
	private AnimationManager animationManager;
	
	
	public Ghost(int x,int y) {
		
		Image image = new Image("tempGhost.png",50,50,false,false);
		Image[] frames = new Image[1];
		frames[0] = image;
		
		Animation animation = new Animation(frames,1.f);
		Animation[] animations = new Animation[1];
		animations[0] = animation;
		
		animationManager = new AnimationManager(animations);
		
	}
	
	public void update() {
		animationManager.update();
		animationManager.playAction(0);
	}
	
	public void draw(GraphicsContext graphicsContext) {
		
		animationManager.draw(graphicsContext,this.getHitBox().getX(),this.getHitBox().getY());
	}
	
	
}
